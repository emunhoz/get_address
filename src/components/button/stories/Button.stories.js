import React from 'react'
import { Button } from '../Button'

export default { title: 'Button' }

export const primary = () => <Button>Buscar</Button>
export const disabled = () => <Button disabled>Buscar</Button>
